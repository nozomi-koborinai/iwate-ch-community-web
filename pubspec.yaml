name: iwate_ch_community_web
description: いわてちゃんねるのコミュニティだよ

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # 追加
    sdk: flutter         # 追加

  cupertino_icons: ^1.0.2
  intl: any
  image_picker: ^0.8.4+2
  image_picker_web: any
  firebase_core: ^1.7.0
  firebase_core_web: ^1.1.0
  firebase_auth: ^3.1.1
  firebase_auth_web: ^3.1.1
  firebase_storage: ^10.0.3
  firebase_storage_web: ^3.0.3
  cloud_firestore: ^2.5.3
  cloud_firestore_web: ^2.4.3
  image_whisperer: ^0.11.1
  universal_html: ^2.0.8
  firebase: ^9.0.2
  http: ^0.13.4
  path: ^1.8.0
  dotted_border: ^2.0.0+1
  provider: ^4.0.2
  salomon_bottom_bar: ^3.1.0
  share_extend: ^2.0.0
  path_provider: ^2.0.1
  google_sign_in: ^5.2.1
  url_launcher: ^6.0.17

flutter_icons:
  android: true
  ios: true
  image_path: "images/favicon.png"


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
  assets:
    - images/